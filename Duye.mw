{{Infobox Localité
 | nom                       = Duye
 | nom local                 = DOUE
 | nom officiel              = <!-- (valeur par défaut : propriété 1448 de l'élément Wikidata) -->
 | continent                 = Afrik
 | pays                      = Senegaal
 | région                    = Ndar
 | intitulé région           = <!-- permet de changer l'intitulé du champ « Région » -->
 | partie de                 = <!-- (valeur par défaut : propriété 361 de l'élément Wikidata) -->
 | localisation géographique = <!-- (valeur par défaut : propriété 706 de l'élément Wikidata) -->
 | chef-lieu                 = <!-- (valeur par défaut : propriété 36 de l'élément Wikidata) -->
 | capitale de               = <!-- (valeur par défaut : propriété 1376 de l'élément Wikidata) -->
 | exclave de                = <!-- (valeur par défaut :propriété 500 de l'élément Wikidata) -->
 | enclavé dans              = <!-- (valeur par défaut : propriété 501 de l'élément Wikidata) -->
 | baigné par                = <!-- (valeur par défaut : propriété 206 de l'élément Wikidata) -->
 | coordonnées               = <!-- (valeur par défaut : propriété 625 de l'élément Wikidata) -->
 | altitude                  = <!-- (valeur par défaut : propriété 2044 de l'élément Wikidata) -->
 | superficie                = <!-- (valeur par défaut : propriété 2046 de l'élément Wikidata) -->
 | subdivision               = <!-- (valeur par défaut : propriété 150 de l'élément Wikidata) -->
 | division                  = <!--  (valeur par défaut : propriété 131 de l'élément Wikidata) -->
 | nom division              = <!--  (valeur par défaut : P31 l'élément utilisé dans la propriété 131 de l'élément Wikidata) -->
 | division2                 = <!--  (valeur par défaut : indirectement par la propriété 131 de l'élément Wikidata) -->
 | nom division2             =  <!--  (valeur par défaut : indirectement par la propriété 131 de l'élément Wikidata) -->
 | point le plus bas         = <!-- (valeur par défaut : propriété 1589 de l'élément Wikidata) -->
 | point culminant           = <!-- (valeur par défaut : propriété 610 de l'élément Wikidata) -->
 | population                = <!-- (valeur par défaut : propriété 1082 de l'élément Wikidata) -->
 | gentilé                   = <!-- (valeur par défaut : propriété 1549 de l'élément Wikidata) -->
 | taux de chômage           = <!-- (valeur par défaut : propriété 1198 de l'élément Wikidata) -->
 | électorat                 = <!-- (valeur par défaut : propriété 1831 de l'élément Wikidata) -->
 | chef de l'exécutif        = <!-- (valeur par défaut : propriété 6 de l'élément Wikidata} -->
 | assemblée                 = <!-- (valeur par défaut : propriété 194 de l'élément Wikidata) -->
 | monnaie                   = <!-- (valeur par défaut : propriété 38 de l'élément Wikidata) -->
 | équipement                = <!-- (valeur par défaut : propriété 912 de l'élément Wikidata) -->
 | employés                  = <!-- (valeur par défaut : propriété 1128 de l'élément Wikidata) -->
 | diocèse                   = <!-- (valeur par défaut : propriété 708 de l'élément Wikidata) -->
 | statut patrimonial        = <!-- (valeur par défaut : propriété 1435 de l'élément Wikidata) -->
 | membre de                 = <!-- (valeur par défaut : propriété 463 de l'élément Wikidata) -->
 | jumelage                  = <!-- (valeur par défaut : propriété 190 de l'élément Wikidata) -->
 | origine du nom            = <!-- (valeur par défaut : 138 de l'élément Wikidata) -->
 | concepteur                = <!-- (valeur par défaut : propriété 287 de l'élément Wikidata) -->
 | fondation                 = <!-- (valeur par défaut : propriété 571 de l'élément Wikidata) -->
 | fondateur                 = <!-- (valeur par défaut : propriété 112 de l'élément Wikidata) -->
 | architecte                = <!-- (valeur par défaut : propriété 84 de l'élément Wikidata) -->
 | remplace                  = <!-- (valeur par défaut : propriété 1365 de l'élément Wikidata) -->
 | événement clé             = <!-- (valeur par défaut : propriété 793 de l'élément Wikidata) -->
 | histoire                  = <!-- (valeur par défaut : propriété 2184 de l'élément Wikidata) -->
 | biens patrimoniaux        = <!-- (valeur par défaut : propriété 1456 de l'élément Wikidata) -->
 | langue officielle         = <!-- (valeur par défaut : propriété 37 de l'élément Wikidata) -->
 | devise                    = <!-- (valeur par défaut : propriété 1451 de l'élément Wikidata) -->
 | blasonnement              = <!-- (valeur par défaut : propriété 237 de l'élément Wikidata) -->
 | blason                    = <!-- (valeur par défaut : propriété 94 de l'élément Wikidata) -->
 | drapeau                   = <!-- (valeur par défaut : propriété 163 de l'élément Wikidata) -->
 | symbole                   = <!-- (valeur par défaut : propriété 2238 de l'élément Wikidata) -->
 | fête                      = <!-- (valeur par défaut : propriété 841 de l'élément Wikidata) -->
 | saint patron              = <!-- (valeur par défaut : propriété 417 de l'élément Wikidata) -->
 | plat traditionnel         = <!-- (valeur par défaut : propriété 868 de l'élément Wikidata) -->
 | code postal               = <!-- (valeur par défaut : propriété 281 de l'élément Wikidata) -->
 | indicatif téléphonique    = <!-- (valeur par défaut : propriété 473 de l'élément Wikidata) -->
 | représenté par            = <!-- (valeur par défaut : propriété 1875 de l'élément Wikidata) -->
 | distinction               = <!-- (valeur par défaut : propriété 166 de l'élément Wikidata) -->
 | site web                  = <!-- (valeur par défaut : propriété 856 de l'élément Wikidata) -->
 | géolocalisation           = <!-- (valeur par défaut : la carte existante la mieux centrée sur les coordonnées, ce qui n'est qu'une estimation grossière de la carte adaptée, notamment près des frontières) -->
}}

ko saare toksere wonnde hedde [[Podoor]] goɗal 12 km jeyaande e peccal [[Ndar]] [[Senegaal ]]
tumɓɓinde hakkonde maaje ɗiɗi: maayo senegal e maayo Duye

==Taariik mayre==
Duye cosaa ko hitaande 1200i 

==Fooɓre inde Duye==
habraama wonde ummii ko e inde Duunde 

==Nguurndam renndoyankooru==
